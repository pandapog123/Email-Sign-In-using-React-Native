# Email Sign In React Native

## Commands to run:

- `npx create-expo-app EmailSignInReactNative -t expo-template-blank-typescript`
- `npx expo customize metro. config.js`
- `npm install @expo/vector-icons firebase`
